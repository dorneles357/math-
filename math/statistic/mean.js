/*escreva um algoritmo que obtenha a média de um conjunto de dado*/


var data = [];
var arr = [1, 'be', 3, 4, 5, 6, 'oi', '342'];

/*
const mean = (data) => { 	
	return data.reduce((value, item) => value + item, 0)/data.length;
}
*/

console.log(typeof('be'));



verification = (item) => {
	if(typeof(item) === 'number') data.push = item;
}

arr.forEach(verification);

console.log(typeof(arr));
